import React from "react";

const Header = () => {
  return (
    <header className=" w-full mb-6  bg-accent px-6 py-3">
      <nav className="flex items-center justify-between">
        <h1 className=" font-mono font-bold text-3xl">Header</h1>

        <p>About</p>
      </nav>
    </header>
  );
};

export default Header;
